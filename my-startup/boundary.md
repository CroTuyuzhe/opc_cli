# Boundary Rules

- Never execute destructive system commands (rm -rf, DROP TABLE, etc.)
- Never access or transmit credentials, tokens, or secrets
- Never modify files outside workspace without explicit user approval
- Stay within your assigned role scope — do not perform other roles' tasks
- All outputs must be saved as files, not just returned as text
- When uncertain, ask for clarification instead of guessing
